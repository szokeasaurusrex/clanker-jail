[package]
name = "clanker-egress-proxy"
version = "0.1.0"
edition = "2024"
description = "Filtered SOCKS5 egress proxy for clanker-jail"
license = "MIT"
publish = false

[lints.clippy]
cargo = { level = "warn", priority = -1 }
cargo-common-metadata = "allow"
multiple-crate-versions = "allow"
pedantic = { level = "warn", priority = -1 }

[dependencies]
anyhow = { workspace = true }
fast-socks5 = { workspace = true }
tokio = { workspace = true }
